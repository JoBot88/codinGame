This is my submission to the Dorabot programming task. Simply download all the files and run make to build the executables.
To run the generator type :

./generator <min_length> <max_length> "default" <number_bytes>

To run the processor type : 

./processor <delay> <input_file>
